<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/rootLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:gravity="center_horizontal"
    tools:context=".MainActivity">

    <TextView
        android:id="@+id/titleTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Cell &amp; Location Info (Auto-Updating)"
        android:textSize="24sp"
        android:textStyle="bold"
        android:layout_marginBottom="24dp" />

    <ScrollView
        android:id="@+id/scrollView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginBottom="16dp"
        android:elevation="4dp">

        <TextView
            android:id="@+id/infoTextView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Waiting for data...\nEnsure permissions are granted and location is enabled."
            android:textSize="16sp"
            android:fontFamily="monospace"
            android:padding="8dp"
            android:background="@drawable/rounded_log_background"
            android:textColor="@android:color/white" />
    </ScrollView>

    <ImageView
        android:id="@+id/myImageView"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:layout_marginBottom="16dp"
        android:adjustViewBounds="true"
        android:contentDescription="A beautiful image of Parvin"
        android:scaleType="fitCenter"
        android:src="@drawable/logo" />

    <CheckBox
        android:id="@+id/captureDownloadRateCheckBox"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Capture Download Rate"
        android:checked="true"
        android:layout_marginBottom="8dp"
        android:textSize="16sp" />

    <CheckBox
        android:id="@+id/capturePingTestCheckBox"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Capture Ping Test"
        android:checked="true"
        android:layout_marginBottom="8dp"
        android:textSize="16sp" />

    <CheckBox
        android:id="@+id/captureSmsTestCheckBox"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Capture SMS Test (Every 10 Entries)"
        android:checked="true"
        android:layout_marginBottom="16dp"
        android:textSize="16sp" />

    <Button
        android:id="@+id/toggleButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="START"
        android:paddingStart="24dp"
        android:paddingEnd="24dp"
        android:paddingTop="12dp"
        android:paddingBottom="12dp"
        android:textSize="18sp"
        android:textColor="@android:color/white"
        android:backgroundTint="@color/blue_500"
        android:layout_marginTop="16dp"
        android:layout_marginBottom="16dp"
        app:cornerRadius="8dp" />

</LinearLayout>
